apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: will-ingress
  namespace: dev
  annotations:
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: will-service
                port:
                  number: 80